#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pi pair<int, int>
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t-- > 0){
        int l, r, m;
        cin >> l >> r >> m;
        int a = l, b, c;
        while (true){
            int r = m % a;
            if (r <= (r - l) || ) 
        }
    }
    return 0;
}