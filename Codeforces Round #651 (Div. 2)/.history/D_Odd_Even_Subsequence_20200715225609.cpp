#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pi pair<int, int>
void p(vector<pi> n){
    for (pi p : n ){
        cout << p.first << " " << p.second << endl;
    }
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    vector<int> nums(n);
    for (int i = 0; i < n; i++){
        cin >> nums[i];
    }
    int lo = 1, 
    return 0;
}